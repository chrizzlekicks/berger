[package]
name = "bergr"
version = "0.1.0"
edition = "2024"
description = "Make tmux sessions agent-aware from Claude Code lifecycle hooks"

[dependencies]
serde = { version = "1", features = ["derive"] }
serde_json = "1"

[dev-dependencies]
tempfile = "3"
